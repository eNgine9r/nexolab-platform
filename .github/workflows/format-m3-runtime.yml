name: Format M3 runtime

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/format-m3-runtime.yml"
      - "src/lib/telemetry/runtime.ts"

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - name: Install dependencies
        env:
          HUSKY: 0
        run: npm install --no-audit
      - name: Format runtime
        run: npx prettier --write src/lib/telemetry/runtime.ts
      - name: Commit formatter output
        run: |
          if git diff --quiet; then
            echo "No formatting changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/lib/telemetry/runtime.ts
          git commit -m "style: format telemetry runtime"
          git push origin HEAD:${{ github.head_ref }}
