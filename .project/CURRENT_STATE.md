# NEXOLAB Current State

Updated: 2026-09-03

## Current Sprint

`PRODUCTION-READINESS-1` remains active. TG-04 / Issue #825 is the active product acceptance work again after state-only reconciliation Issue #851 completed its source-authority gate following the Product Owner-approved Stage-2 production cutover. Exact product source `13ab26392fcb1c1385dca3c1f619da4512fe568c` deployed successfully on the controlled Raspberry Pi with authoritative evidence `runtime/deployments/20260903T191159Z`; live PostgreSQL is `20260902_0031`. Dashboard, Telemetry, MQTT and Device Agent are healthy, persistent Docker volume identities are preserved, Telegram delivery remains disabled, the Telegram worker remains stopped with `last_send_at=null`, Mini App remains enabled, and `DAILY_REPORTS_SCHEDULER_ENABLED=false`. Tailscale `:8444` now proxies the deployed Dashboard on `:3000`.

Version-management source adoption is now recorded successfully through the canonical repository-owned adopter: `controlled_source_deployment`, source `13ab26392fcb1c1385dca3c1f619da4512fe568c`, schema `20260902_0031`, evidence `runtime/deployments/20260903T191159Z`, health `ready`, platform `linux/arm64`, runtime mode `lan`, `known_packaged_release=false`. Issue #851 is therefore completion-ready and TG-04 / #825 resumes as the active product acceptance work. Telegram delivery and the weekday 07:50 scheduler remain OFF. No further production cutover is authorized by the consumed Stage-2 approval. RFX-01 through RFX-19C remain on the Product Owner presentation hold.

## Issue #851 — Stage-2 deployment reconciliation active

Stage-2 was authorized explicitly for exact source `13ab26392fcb1c1385dca3c1f619da4512fe568c` using verified ARM64 artifact `9905912342`, with Telegram delivery and the weekday 07:50 scheduler required to remain OFF. The successful run started from prior deployed source `893573a8...`, created the PostgreSQL pre-upgrade backup, captured a consistent edge SQLite snapshot (`sha256:a520569b5d2810e305d8f6d8a48512b0290b09916d150d57691600306e7d41b1`, queue `0`, source/snapshot quick-check OK), preserved the exact deployed Device Agent recovery image, verified an isolated frontend candidate, then activated central/edge/frontend runtime and passed readiness, central smoke and API-contract gates. `final-state.txt` records deployed commit `13ab2639...`, build ID `9DMwe5ReCgEwXr5xJyiy0` and deployment time `2026-09-03T22:21:21+03:00`.

Post-cutover checks confirm Alembic `20260902_0031 (head)`, Telegram Gateway `delivery_enabled=false`, `miniapp_enabled=true`, `running=false`, `last_send_at=null`, scheduler env `DAILY_REPORTS_SCHEDULER_ENABLED=false`, and tailnet-only `:8444 -> http://172.18.48.66:3000`. Before/after persistent-volume identity snapshots are identical. The first attempt `20260903T190544Z` failed before runtime mutation during evidence archival; the later successful `20260903T191159Z` is authoritative. Later `20260903T191835Z` and `20260903T192908Z` attempts failed before mutation and do not supersede it.

#851 is state-only: only canonical `.project` files changed. The canonical privileged adopter succeeded with `status=recorded`, source `13ab2639...`, schema `20260902_0031`, evidence `runtime/deployments/20260903T191159Z`, health `ready`, `linux/arm64`, runtime mode `lan`, and `known_packaged_release=false`. The first state head passed CI run `33798476536`; this completion candidate returns #825 to active for signed Mini App persisted-report acceptance. No Telegram send, schedule enablement, Modbus/hardware write, product-data deletion or volume deletion belongs to #851.

## Issue #849 — TG-04 exact-main Stage-2 pre-cutover reconciliation completed

Issue #849 / PR #850 completed the repository-only pre-cutover reconciliation. Exact-main artifact `9905912342` / run `33786462681`, source-selection, capacity and runtime-contract evidence were recorded; final state passed State Integrity and NEXOLAB Merge Gate and PR #850 squash-merged to `main` at `8f2d349ed4f2680e9aa4872324a9c64f890be305`. The separate Product Owner production-cutover gate was subsequently granted and consumed by the successful Stage-2 deployment now tracked under #851.

## Issue #843 — Telemetry CI determinism repair repository-complete

TG-04 / #825 remains the product work in progress on draft PR #842, where exact head `06b0ec59ab4ba3e380c5fca14d39eede81c058d3` exposed a reproducible pre-existing PostgreSQL planner assertion. The same Telemetry/product code had passed immediately before on the same PostgreSQL 16.15/runner baseline, while the failing full query remained bounded at roughly 0.4–0.5 ms, confirming optimizer-choice nondeterminism rather than a TG-04 product regression.

Issue #843 is repository-complete on exact verified implementation head `fcd9d2429ef19c9410fdfe9292d6f5d04cdc7c1b` / PR #844. The focused repair changes only the PostgreSQL acceptance test: canonical `ix_telemetry_latest_lookup` existence and ordered keys `node_id, equipment_id, channel_id, metric, captured_at, event_id` are verified independently; a constant latest-value probe must remain index-backed without dictating which valid index PostgreSQL chooses; and the production-equivalent full-query timing/result/taxonomy assertions remain intact. Local isolated PostgreSQL 16 evidence is GREEN for three repeated focused runs plus the relevant inventory API/PostgreSQL pair, with a transactional negative proof that the canonical-index catalog contract fails when the index is temporarily removed. Exact-head GitHub verification is GREEN for all 11 registered workflows, including Telemetry Service run `33770161208`, Core CI/NEXOLAB Merge Gate run `33770159789`, Offline Bundle `33770159680`, Container Supply Chain `33770159938`, Offline Auth, Device Agent Fleet, MQTT TLS Fleet, Broker Control, Capacity Release, DR TLS Fleet and DR Browser. Product query, schema, production runtime, TG-04 runtime, Modbus and hardware behavior are unchanged. PR merge status remains a volatile GitHub observation; after merge, resume `ops/825-telegram-real-acceptance` from current `main` without force-push.

## Issue #845 — TG-04 scheduler fail-closed prerequisite

TG-04 Stage 1 Mini App runtime acceptance passed on the controlled Raspberry Pi at source `861b9f83670a53a7acc4cdecebaae61f5fe57980`: the loopback Telegram Gateway is healthy with `delivery_enabled=false`, `miniapp_enabled=true`, worker `running=false`, and unchanged core-container/Tailscale topology. No report was sent and the weekday schedule was not enabled. Production PostgreSQL remains on schema `20260902_0030`, so persisted TG-01 snapshots are not yet available in the deployed backend.

Stage-2 preflight stopped before mutation after discovering unsafe scheduler defaults. Issue #845 / PR #846 fixed application, service-constructor and central Compose defaults to fail-closed OFF while preserving later explicit enablement; exact head `cae3e45ad6a11b74797dd843c187c44602f9aa81` passed the full classified matrix and squash-merged to `main` at `242dd46c01109e2f75b7ac60e5bf8f7f28ac49b6`. Production source authority remains `893573a86faf6629e5e3cefc0e41c47230a7f8c7`; no migration/cutover occurred.

## Issue #847 — scheduler-gate state reconciliation repository-complete

Issue #847 is repository-complete at exact verified state head `715d0da1d634713c7dcccf216252b0e54f12ab30` / PR #848 first state gate: State integrity and NEXOLAB Merge Gate were GREEN in Core run `33781466896`. Durable state records #845 completed, restores TG-04 / #825 to active, and preserves production source `893573a86faf6629e5e3cefc0e41c47230a7f8c7` / schema `20260902_0030`. This state reconciliation performed no runtime, database, Telegram-send, scheduling, Modbus/hardware, data-deletion or volume mutation.

## TG-03 / Issue #824 — completed and merged

TG-03 is repository-complete. PR #839 contains the focused read-only `/telegram-miniapp` surface, same-origin bounded proxy, server-side Telegram `initData` HMAC/age/start-parameter validation, strict Telegram-user-to-NEXOLAB identity links, exact organization/RBAC re-authorization in Telemetry Service, persisted TG-01 snapshot rendering, explicit unavailable states, M-packet Tmin/Tmax/coverage, compressor coverage, interval energy evidence, defrost duration only, alerts/quality, and optional loopback-only gateway wiring. It does not trust `initDataUnsafe`, browser actor/user/organization IDs or client `startapp` as authority. The Telegram direct-link template is constrained to exact `startapp=report_{snapshot_id}` and the host proxy is constrained to the fixed private gateway endpoint.

Local verification was GREEN: gateway `38/38`; focused Telemetry report/security `8/8`; frontend Vitest `142` files / `693` tests; Telegram Mini App mobile Playwright `1/1` at `390×844`; full ESLint; `tsc --noEmit`; Python compile with isolated pycache; Compose overlay contract; Prettier; State Model v2; `git diff --check`; secret-pattern scan `0`; and Next.js 16.2.12 production build. Exact product head `31cd4fd5e43672b81af34c8ac1f53e185cf4752e` passed Core CI / NEXOLAB Merge Gate (`33721582258`), Telegram Gateway (`33721582220`), Container Supply Chain (`33721582184`), Offline Bundle (`33721582434`), Authenticated Dashboard (`33721582470`), Telemetry Service (`33721582242`) and Security Browser (`33721582198`). A navigation regression found by Authenticated Dashboard was fixed before acceptance: ordinary route transitions retain the shared telemetry shell while `/telegram-miniapp` remains isolated. The final state head `e95b8107919d5f12ce32fc90800ff6cd7f71870f` was reverified across the full registered PR matrix, including Offline Bundle and NEXOLAB Merge Gate, before squash merge.

No real Telegram token/group contact, BotFather configuration, secret provisioning, production deployment/restart, Modbus/hardware write, product-data deletion or volume deletion occurred in TG-03. Real Telegram WebView/Tailscale/group acceptance belongs to TG-04.

## TG-04 / Issue #825 — Stage 2 deployed; acceptance remains open

TG-04 prerequisites TG-01/TG-02/TG-03 are merged GREEN. Real Bot API/group discovery, protected runtime secrets, least-privilege backend principal, administrator Telegram identity link, tailnet HTTPS and real phone WebView prerequisites are confirmed. PR #842 merged the repaired Main Mini App UI and guarded runtime/provisioning helpers to `main` at `13ab26392fcb1c1385dca3c1f619da4512fe568c`; exact ARM64 artifact `9905912342` was verified before deployment.

Stage-2 production deployment is now **complete** with authoritative evidence `runtime/deployments/20260903T191159Z`. Live schema is `20260902_0031`, so persisted TG-01 snapshots are available in the deployed backend. Deployment preserved persistent volumes and the edge snapshot/recovery boundary, and post-cutover runtime is healthy. Telegram delivery is still OFF, worker is OFF, no report has been sent, and the weekday scheduler is still OFF. The Mini App tailnet endpoint `:8444` is mapped to the deployed dashboard rather than the retired Stage-1 candidate.

The immediate prerequisite before resuming #825 acceptance is #851 metadata reconciliation: canonical source adoption must record `13ab2639...` / `20260902_0031` from the successful deployment evidence. After that, #825 may resume with one exact persisted report through signed Telegram `initData`. Any actual TestLAB group send is a later explicit acceptance step; Monday-Friday 07:50 `Europe/Kyiv` enablement remains later still. Modbus/controller writes remain forbidden.

## Telegram morning reports — TG-01 / Issue #822 completed

TG-01 / Issue #822 is **completed**. PR #828 exact verified head `dede5e5d4680d89a5404baa0f327242e7f7816ce` passed all 20 registered exact-head PR workflows, including Core CI / NEXOLAB Merge Gate, Telemetry Service, Refrigeration Browser, Authenticated Dashboard and Offline Bundle, then merged to `main`. The accepted local/offline-first report domain persists immutable/idempotent 07:50 `Europe/Kyiv` snapshots, keeps M-packet Tmin/Tmax semantics bounded to current valid configured M-packet channels, exposes defrost duration only, fails closed on ambiguous energy/coverage boundaries and leaves deferred thermodynamic values typed unavailable. No Telegram/Bot API call, public listener, Device Agent change, Modbus write, hardware write or production deployment belonged to TG-01.

TG-02 / Issue #823 is **completed and merged** through PR #830 at exact verified head `25f3857f291bcada05bec09b2d8d7323c52a3b22`. Final post-#834 verification is GREEN for Core CI / NEXOLAB Merge Gate (`33691029025`), Telegram Gateway (`33691029042`), Container Supply Chain (`33691029067`), Offline Bundle (`33691029039`), Authenticated Dashboard (`33691029064`), Refrigeration Browser (`33691029079`) and Telemetry Service (`33691029038`), with zero unresolved review threads. The optional gateway reads persisted TG-01 snapshots only through an authenticated private/local NEXOLAB API boundary, renders bounded Ukrainian group messages, persists retry/idempotency in a nonroot permission-hardened SQLite outbox and sends only through outbound Bot API HTTPS. Runtime uses `gcr.io/distroless/python3-debian13:nonroot`; TG-02 retains 17 exact HIGH-only gateway exceptions through the consolidated 2026-09-05 review. No real Telegram token/group contact, production deployment, Device Agent/Modbus change or hardware write occurred. TG-03 / Issue #824 is completed and merged through PR #839.

## Supported-device onboarding — #805 production acceptance completed

Supported-device onboarding acceptance is **completed** through Issue #805. After #809 restored non-destructive source authority and #826/#827 corrected unsupported-wizard navigation, the Product Owner-authorized historical-main deployment activated exact source `893573a86faf6629e5e3cefc0e41c47230a7f8c7` with `DEPLOYMENT PASSED` evidence at `runtime/deployments/20260902T113916Z`. Live schema remains `20260902_0030`; persistent volume identities were preserved; Dashboard, Telemetry, MQTT and Device Agent are healthy with queue 0, workers 2/2 and advancing samples.

Authenticated production Chromium acceptance used the same persisted unsupported commissioning draft across browser reload, Connections-list reopen and the real Dashboard/Telemetry restart performed by controlled deployment. On the corrected runtime, `Далі`, Steps 2–5 and SAFE PREFLIGHT remain disabled for `Unsupported / Profile required`, activation is absent, and the acceptance draft was finally cancelled through the normal audit-preserving UI path. Real Embraco Unit 2 preflight on the corrected source passed in 93 ms using the exact stable adapter, Unit ID 2 and repository-owned profile; evidence is `hardware_verified`, FC03 only, with Modbus writes = none and hardware writes = none. No onboarding activation was required or executed.

Controlled source adoption is recorded for `893573a86faf6629e5e3cefc0e41c47230a7f8c7`, `linux/arm64`, LOCAL_LAN, schema `20260902_0030`, health `ready`, `known_packaged_release=false`, evidence `runtime/deployments/20260902T113916Z`. Browser resources/runtime contract remain local-LAN/same-origin with no mandatory public/CDN dependency.

## Issue #792 — selected-interval relay traceability, compressor starts and CSV export

Issue #792 is **completed**. PR #793 passed exact-head Core Quality/build, Refrigeration Browser Acceptance, Authenticated Dashboard Acceptance, Disaster Recovery Browser and NEXOLAB Merge Gate, then squash-merged to `main` at observed GitHub merge SHA `5e535fed3066d257ea0f0a5342dcbf2eb96a0632`; GitHub closed Issue #792 as completed. The accepted operator behavior is direct left-button drag on the compressor plot to select one analysis interval for compressor duty/coverage, confirmed compressor starts, control state, generic Relay 1..4 lanes, relay-transition provenance, alarm filtering and local UTF-8 CSV export. The selection remains highlighted after release and `Скинути вибір` restores the full loaded range.

Reviewed product/test head `cb6bd4357f32e5655d8ebba523afc0bd25c9e842` had 43/43 exact changed tests PASS and all required remote lanes GREEN. Final state-only PR head `de525bde48d29202e684e36a7ba5787c1a3aff0e` was also reverified GREEN across Core/Merge Gate, Refrigeration Browser, Authenticated Dashboard and Disaster Recovery Browser. The DR repair verifies restored canonical equipment identity from the API against the operator H1 rather than a stale hard-coded title. No backend schema, telemetry acquisition, polling cadence, controller configuration, Modbus behavior, hardware write or production deployment changed in #792.

## Issue #794 — post-#792 state reconciliation

This state-only Work Package reconciles the durable State Model v2 with GitHub after #792 merged: #792 is completed, the stale active selection is cleared, existing recovery/hardware blockers and the RFX Product Owner hold remain unchanged, and no runtime/product/hardware mutation is performed. Repository-side reconciliation is self-completing in this state candidate; GitHub remains authoritative for the PR/Issue merge result when online.

## Issue #790 — controlled deployment final Device Agent health race

Issue #790 is **repository-complete** on PR #796 at exact verified implementation head `cd01e27054f30af85f10be0a49e13524e285abe0`. Core Quality/build and NEXOLAB Merge Gate are GREEN in run `33512424465`, and all deployment-health review threads are resolved. The final gate preserves its helper across historical `--source-ref` checkout, holds the same container identity through one bounded convergence deadline, retries Docker plus operational readiness without accepting terminal states, requires complete scheduler evidence with matching non-negative integer worker counts, and passes the actual remaining deadline into every blocking `docker ps`, `docker inspect` and `/health` call. Docker subprocess timeout is translated into fail-closed `HealthGateError`, so a stalled Docker daemon cannot leave post-mutation deployment authority indeterminate.

Local final verification is GREEN: 18/18 deterministic health-gate tests, 47/47 deployment regressions, 33/33 change-impact regressions, standalone offline runtime contract, Python compile, shell syntax, State Model v2 and `git diff --check`; read-only smoke against the already-running Device Agent remained healthy without mutation. No production deployment/restart, source-authority advancement, Modbus/hardware write, acquisition/polling/configuration change or data/volume deletion occurred. Failed #789 evidence remains failed as historical evidence; the later successful #805 controlled deployment superseded that deployed/source authority with `893573a8...`.

## Issue #789 — production runtime acceptance for #785

The Product Owner explicitly authorized the controlled LOCAL_LAN rollout and on **2026-09-01** confirmed in the real NEXOLAB interface that the compressor range selector and selected-interval KPI recalculation work as intended. Verified ARM64 frontend workflow `33477804826` built exact runtime target `70932c27f373841358971dd9aa568551fc21629a`; source-lineage preflight from authoritative deployed source `20bb9ca473395a0c64267b9b08523c31404f41e6` passed. Deployment attempt evidence is `runtime/deployments/20260901T064156Z`; its pre-cutover edge SQLite snapshot SHA-256 is `5ba126fd4e24932901a9b80a4657e6dd400a915d6a988a20437e5bb01f445930`.

The attempt activated the new Dashboard and central/edge runtime, and post-attempt observation is healthy: Dashboard HTTP 200; Device Agent Docker `healthy` in read-only `modbus` mode; MQTT connected; queue depth 0; expected/active bus workers `2/2`; Embraco Unit 2 continues successful FC03 reads on `rs485-embraco`. However, the deployment script exited before writing `DEPLOYMENT PASSED` because the final evidence check observed Device Agent Docker health before it converged from startup to `healthy`. Therefore **formal controlled-source authority is not advanced by this attempt** and remains anchored to `20bb9ca...` / the prior authoritative deployment. Issue #790 tracks the deterministic health-gate race. #789 is product-complete by explicit real operator acceptance, without misrepresenting the failed final deployment-evidence gate. No Modbus write, hardware write, polling change, controller configuration change, persistent-data deletion or volume deletion occurred.

## Issue #785 — operator-selected compressor analysis interval

The Product Owner explicitly selected Issue #785 as a presentation-readiness product Work Package on **2026-09-01**. The accepted compressor runtime formula remains unchanged: time-weighted `compressor.speed`, `RPM > 0` as running, `RPM = 0` as stopped, with invalid evidence and continuity gaps excluded. No defrost/control-state filtering is introduced; the operator is responsible for the content of the selected interval.

Implementation on `feat/785-compressor-analysis-range` lifts the compressor chart viewport into a shared analysis range, adds an opt-in ECharts range slider only to the compressor chart, recalculates the existing KPI from the selected viewport without refetching telemetry, resets stale selections when the loaded history period changes, and clips arbitrary selection boundaries to evidence-backed adjacent RPM intervals. Local focused verification is GREEN: 31/31 selected refrigeration/chart tests, touched-file Prettier, touched-file ESLint and `tsc --noEmit`. Exact verified product head `5a1df9a08dbe39c4be0f93c6a5e6dc622136d1c3` is GREEN for Core Quality/build, Refrigeration Browser Acceptance, Authenticated Dashboard Acceptance, Disaster Recovery Browser and NEXOLAB Merge Gate. PR #786 squash-merged to `main` on 2026-09-01 and closed Issue #785. No production deployment, Modbus write, hardware write, polling change or telemetry-schema change belongs to #785.

### Product priority hold — RFX-01 through RFX-19C

On **2026-08-28** the Product Owner intentionally deferred the global RFX implementation programme **RFX-01 through RFX-19C for an indefinite period until after the main NEXOLAB presentation** (Issue #727). This is a product-priority decision, not a technical failure or architecture rollback. The current product is the presentation/delivery baseline; normal bug fixes, security maintenance and explicitly requested presentation-readiness work remain allowed.

RFX-01 / Issue #717 is no longer Ready and is blocked by the Product Owner hold. PR #718 was closed **without merge**; its branch and head `61bf4eac3f6c87b92786327e1b63416977057583` are retained only as a recoverable engineering checkpoint. RFX-02 and all later RFX Work Packages must not start automatically. Resume requires an explicit Product Owner restart decision after the presentation followed by a fresh Team Lead audit of current `main`, `.project/**`, architecture, migration head, open Issues/PRs and CI; the pre-pause PR must not be treated as merge-ready by default.

Security/CI interrupts discovered during the attempted RFX-01 verification were completed independently because they affect the presentation baseline: #723 / PR #724 merged at `8e9333fe76bce4a5babccaf7a3bedf35c5fe49bb`, and #719 / PR #721 merged at `1f2654dec0f02263aec6c2314187cfa62e5723e9` with exact-head Container Supply Chain, Telemetry service, Core CI and NEXOLAB Merge Gate GREEN. Issue #722 triage is completed. The exact Device Agent `libexpat1/CVE-2026-66046` maintenance decision remains separately reviewable through **2026-09-02**.

Bounded production deployment #755 successfully activated historical-main target `ff86b10b71c8e5252c15baaf4183adbf42f30f18` on 2026-08-29 with `DEPLOYMENT PASSED` evidence at `runtime/deployments/20260829T154823Z`. This target contains #709 and #711 while intentionally excluding later #729 product/runtime scope. Real runtime acceptance for #709 and #711 is PASS and controlled-source version-management adoption is recorded; #755 runtime work is complete. Existing hardware/recovery blockers remain separately documented.

## Issue #733 — canonical project-state formatter boundary

Repository maintenance now excludes canonical `.project/*.json` State Model v2 files from Prettier formatting while leaving all ordinary formatter behavior unchanged. `scripts/validate-project-state.py` remains the sole formatting authority for `ACTIVE_SPRINT.json` and `LAST_CHECKPOINT.json`. The lint-staged v17 regression harness now stages canonical state JSON, runs the production lint-staged configuration and verifies byte-for-byte staged-state preservation. Formatter-only paths `.prettierignore` and `scripts/tests/lint-staged-v17.mjs` are also classified as known dependency/toolchain changes: they retain full Core quality/build but no longer fail closed into unrelated dashboard/offline/refrigeration external workflow requirements. The change-impact regression matrix passes 29/29. This fixes the deterministic Husky/CI routing conflicts reproduced during Issue #729 checkpointing without changing application or runtime behavior.

## Issue #729 Embraco refrigeration digital twin

Issue #729 is **completed** and PR #736 is merged. The accepted exact PR head is `c30a0d3b6cdb310652f9fc11f817ca2d986f77c4`. Exact-head GitHub verification is GREEN for Core Quality/build, Telemetry service, Refrigeration Browser Acceptance, Authenticated Dashboard Acceptance after one controlled rerun of a transient unchanged-product-code failure, Offline Bundle, Disaster Recovery Browser, Security Browser Acceptance, Device Agent Fleet Acceptance, Container Supply Chain and the NEXOLAB Merge Gate. The GitHub merge result was observed on `main` at `9b9f8cb74e98d3cd0f3162c8a883f02245344333`; that volatile merge/main fact is an observation and does not replace the exact verified product head.

The completed vertical slice adds a strict FC03-only Embraco Sync profile, reusable controller-to-refrigeration-equipment binding, persisted latest/history integration, operator tabs `Огляд / Схема / Графіки / Контролер`, periods `1 год / 12 год / 24 год / Кастом`, state/relay/alarm timelines, and a time-weighted compressor runtime duty calculation with explicit coverage and continuity-gap handling. Temperature/control engineering scaling remains fail-closed until real hardware correlation confirms it; raw values are not presented as verified °C.

No `EMBRACO_UNIT_IDS=2` production activation, production migration, runtime deployment, Modbus write, controller parameter change or hardware write occurred. The earlier local heavy-browser reboot risk is closed as a repository merge blocker because the full exact-head GitHub acceptance is GREEN; it does not authorize production activation.

## Active presentation-readiness chain after #729

Issue #730 is **completed** and PR #731 is merged. Exact verified head `ca030f78e6a2412ec7a993a82ff8542ccd76181d` is GREEN for Core Quality/build, Telemetry service and NEXOLAB Merge Gate; all review threads are resolved, and GitHub closed Issue #730 as completed after squash merge `01443205bfeba28cfcc2192e5d290a9add9b24fe`. Fresh approved-workstation Opera/Tailscale acceptance passed on **2026-08-29**: `/inspection-login` automatically redirects into the normal authenticated shell without Product Owner credential input; Settings identifies `ChatGPT Opera Inspection` with role `Спостерігач`; REST snapshot synchronization and WebSocket are active. The helper runs as isolated non-login OS principal `nexolab-inspection`; its private directory is `0700`, config/credential files are `0600`, and ordinary `nexolab` cannot read the credential. Credential exchange remains behind the root-owned `0600` Unix socket plus exact Tailscale identity/source checks; plain HTTP is loopback-only, non-loopback login targets require HTTPS, DNS HTTPS targets must exactly match `expected_host`, and invalid runtime config fails closed with HTTP 503. Serve remains tailnet-only, direct frontend `/inspection-login` is HTTP 404, a non-approved Pi identity is HTTP 403, and production Dashboard `:3000` remains HTTP 200. No credential/token values were collected and no product/runtime deployment, Modbus write or hardware write occurred.

Issue #725 (`CI-OPT-01`) is completed and PR #743 is merged. Exact verified head `7070ba520f482208beb47f36435a33324ff0c2e0` is GREEN for Core Quality/build, Telemetry service, Authenticated Dashboard Acceptance, Offline Bundle, Refrigeration Browser Acceptance and NEXOLAB Merge Gate. The repository now provides a detached clean-worktree local candidate gate with dependency-free state-only verification, full Core non-state planning, exact Node/NVM fallback, automatic Compose validation for deployment/runtime changes and fail-closed unknown-path handling. A real state-only candidate was verified GREEN. Full non-state local verification is intentionally directed away from the production 4 GB Raspberry Pi when it would risk competing with runtime services.

The post-#730 presentation-readiness chain has now completed #802, #803, #804 and real production acceptance #805, including the independent #826 correction discovered during browser acceptance. TG-01 / #822 is also merged GREEN, so TG-02 / #823 is the next independent Ready software Work Package. #200/#201/#202/#585 remain hardware/evidence-gated and RFX-01 through RFX-19C remain on Product Owner hold. Issue #732 is closed not-planned because #733 established the accepted canonical State Model formatter boundary, and #720 is closed completed because the merged refrigeration/DR acceptance selects the authoritative `Схема` route and exact-head DR verification is GREEN.

## Issue #753 — bounded historical-main deployment authority

The Product Owner explicitly authorized controlled Raspberry Pi deployment/runtime verification for Issues #709 and #711 on **2026-08-29**. The bounded runtime target is `ff86b10b71c8e5252c15baaf4183adbf42f30f18`: current deployed `ff796b1f...` → #709 `3f73e81f...` → #711 `ff86b10b...`. Current `main` contains later product work, including #729 Embraco, that is outside this deployment authorization.

Issue #753 is **completed and merged** in PR #754 at merge SHA `76fa83a80e2eef82ae6f6e7c616a0dbe9352a5c8`. Canonical verified implementation head `b8fb31de3c14ee6c0c000ccbd548adf2f202f4c4` has 51/51 focused safety checks, exact-head Core/Telemetry/NEXOLAB Merge Gate GREEN, fresh Codex review with no major issues, and all deployment-safety threads resolved. The final state correction head `8c144ea0d6a0d7c0ca0ec6ea46d78618f73931d9` is also exact-head GREEN and clean-reviewed. No product runtime mutation occurred in #753.

## Issue #760 — Embraco Unit 2 production activation

Issue #760 production/runtime acceptance is complete for **physical controller #2 only**, mapped to Embraco Unit ID 2 on `rs485-embraco`; controller #1 is test-only and Unit 96 remains excluded. Repository-owned controlled deployment `runtime/deployments/20260830T202942Z` passed on exact source `20bb9ca473395a0c64267b9b08523c31404f41e6` using verified ARM64 frontend artifact run `33330031287` / artifact `9737449091`. Pre-cutover SQLite snapshot SHA-256 `a0d2fdd29d9d51c356476215b8d25aa9a8c8a79915716cdfd684ba5c5c9e56ab` captured revision 18 with quick-check OK. Post-cutover schema head is `20260828_0027`; persisted registry revision 20 has `embraco-2` active on `rs485-embraco` while LE-01MP Unit 201 remains disabled on `rs485-main`.

Real Unit 2 FC03-only telemetry is live and durable. Device Agent is healthy in `modbus` mode, MQTT connected, queue depth zero. PostgreSQL latest/history contains verified compressor speed, control state, relay bits and alarm bits. Temperature, setpoint and hysteresis remain NULL/`unknown`, so unverified engineering scaling is not presented as production data. `Cool jet` is bound to `EMBRACO-2` through the domain repository with security audit event `abe4e8a9-bd8b-4e35-b182-06fd6c8330a3`. Long-run evidence observed 9,191 Embraco rows; the one-hour compressor-speed window had 60 valid samples, max gap ~60.0 s, ~98.3% observed coverage and ~59.3% duty.

Source/version authority is now recorded successfully as `controlled_source_deployment` for exact source `20bb9ca...`, linux/arm64, LOCAL_LAN, schema `20260828_0027`, health `ready`, evidence `runtime/deployments/20260830T202942Z`. For browser acceptance, exact-source tailnet inspection artifact run `33386182467` / artifact `9755897067` was verified (linux/arm64; HTTPS API; WSS telemetry; local auth; 17,125 runtime-file hashes) and served on `127.0.0.1:3102`. Opera `/inspection-login` automatically authenticated `ChatGPT Opera Inspection / viewer`; REST snapshot synchronized and WebSocket was active. The real `Cool jet` production page showed `Embraco Online`, `Cooling`, compressor 2142 rpm / Running, relay states, no controller alarms, and cabinet/evaporator/condenser as `— / Scale unverified`. The Connector cannot press client-state tab buttons, so interactive tab/history behavior is covered separately by the exact-head Refrigeration Browser Acceptance workflow, which is GREEN.

Acceptance PR #779 changed only canonical `.project/**` state. Final state head `46cefa4949390e4747d6c9162a17d8831d6fd31c` passed exact-head State integrity and NEXOLAB Merge Gate, then PR #779 squash-merged to `main` at `72d67360b198e78322b0e2c2dfec5447e02856a2`, closing Issue #760 as completed. No Modbus write, controller/hardware write, destructive database restore or product-data deletion occurred.

## Issue #772 — persisted Embraco physical-bus identity

Issue #772 is completed and PR #773 merged GREEN. Exact candidate head `6bdb215de6a54de6aa0fc52f4089347e8bdfa556` passed focused and full Device Agent verification plus production-copy reproduction. The fix parses explicit bus topology before registry load and atomically persists topology-aware Embraco enrollment, proving `embraco-2.bus_id=rs485-embraco` after first load and restart while preserving Bus 1 lifecycle/cadence, Unit 201 disabled state and legacy single-bus compatibility.

## Issue #755 — bounded #709 + #711 runtime activation and acceptance

Issue #755 runtime work is **complete**. The Product Owner-authorized bounded deployment passed at `runtime/deployments/20260829T154823Z` on exact historical-main source `ff86b10b71c8e5252c15baaf4183adbf42f30f18`, intentionally excluding #729 and later product scope. Post-cutover Telemetry, PostgreSQL, MQTT, Device Agent and Dashboard health are GREEN. The verified ARM64 frontend release is `ff86b10b...-20260829T154823Z`; candidate verification used isolated port `3101` because the persistent Opera inspection frontend legitimately owns `3100`.

Issue #709 production acceptance is PASS on the existing Saved Dashboard `Issue 369 Pi Acceptance`: 359 real export rows over 2h, including 120 numeric valid `108-01 / temperature.probe` rows keyed as `K108` with 24.2–24.6 °C values; inventory/latest independently resolves `K108` at 24.4 °C valid. Issue #709 is closed completed. Issue #711 production acceptance is PASS on 8,573 raw 24h active-power rows: exact cadence-aware target logic reduces to 717 render rows with 7 durable breaks, 0 inferred breaks, preserving genuine long silent/runtime gaps while eliminating the former normal-jitter false segmentation.

Controlled-source version authority is also recorded successfully from canonical `main`: `deployment_authority=controlled_source_deployment`, `source_commit=ff86b10b...`, `schema_head=20260820_0026`, `platform=linux/arm64`, `runtime_mode=lan`, `health=ready`, `known_packaged_release=false`. This metadata adoption did not restart runtime or mutate PostgreSQL, MQTT, Device Agent or industrial equipment. No Modbus write, hardware write, cadence mutation, product-data deletion or named-volume deletion occurred in #755.

## Issue #757 — PostgreSQL compressed-backup capacity prerequisite

Issue #757 is completed and merged in PR #758. The capacity guard measures the read-only compressed `pg_dump -Fc` stream with the existing safety margin, rejects inherited cache authority, preserves conservative `pg_database_size()` fallback, and keeps all prior reserve/build thresholds. Implementation verification remained 14/14 capacity tests, 55/55 focused deployment safety, exact-head Core/Telemetry/NEXOLAB Merge Gate GREEN and clean manual review. The merged correction enabled the successful #755 bounded deployment without deleting protected evidence, recovery assets, Docker named volumes or product data.

## Recently completed production-readiness boundaries

Issue #245 is completed with real Raspberry Pi standalone hardware evidence. Issue #444 LOCAL_LAN user administration, #646 protected `main`, #667 CVE lifecycle reconciliation and #673 state reconciliation are also completed.

## Durable baselines

Latest accepted product/software baseline is TG-03 exact verified PR product head `31cd4fd5e43672b81af34c8ac1f53e185cf4752e`. The currently deployed/physically accepted production source is historical-main onboarding target `893573a86faf6629e5e3cefc0e41c47230a7f8c7`, intentionally older than the accepted TG-03 software baseline because TG-01 through TG-03 were not part of the bounded #805 deployment authorization.

Controlled-source version authority is recorded for `893573a86faf6629e5e3cefc0e41c47230a7f8c7`, evidence `runtime/deployments/20260902T113916Z`, schema `20260902_0030`, `linux/arm64`, LOCAL_LAN, health `ready`. Real authorized Embraco Unit 2 read-only hardware evidence is anchored to the same deployed source. Temperature/control engineering scaling remains explicitly unverified where existing production semantics are unknown. Repository synchronization or later TG-01 merge does not imply deployment of schema `20260902_0031`.

## Issue #675 source-to-packaged authority

Software implementation adds one bounded `establish-package-authority` host command. It accepts only trusted `controlled_source_deployment` lineage and an exact host-validated staged bundle with matching source commit, platform, schema, runtime mode and local-auth boundary. It holds the worker and update-plane locks, requires capacity and a verified non-empty PostgreSQL backup, records persistent-volume identities, preserves hardware/bridge/standalone overlays, performs a rollback-aware source-to-packaged Dashboard handoff, requires exactly one Alembic head, proves the real Modbus path on the same stable RS-485 topology, and commits catalog-backed packaged authority only after volume identities remain unchanged. The packaged record carries forward hardware authority so later update/rollback operations must retain the hardware overlay and re-prove the same hardware contract.

Legacy controlled-source records may derive missing Dashboard/auth identity only from their exact immutable deployment evidence with matching source commit and runtime mode. The full version-management matrix passes 63/63; Python compile, shell syntax and `git diff --check` also pass. Exact-source runtime packaging is now decoupled from recovery tooling through digest-bound `source_commit`/`tooling_commit` provenance, and verified offline image references are activated before any Compose-based backup or post-install verification. Exact-head CI, Telemetry service, Offline Bundle and NEXOLAB Merge Gate were GREEN for the completed #675 implementation. Actual packaged installation has not been executed on the Raspberry Pi.

## Issues #677 / #679 ARM64 package acceptance

#677 is merged and completed at PR #678. Its first real post-merge ARM64/local-auth run `32812878575` correctly failed closed on the QEMU-specific Device Agent Docker health timing gap, which was fixed by #679 without changing the pinned runtime source or native production health semantics. #679 merged at PR #680 and replacement run `32832798392` is GREEN for tooling `431f2a28a8ebf7b86536e5059b381b75d2c5b1a3`, exact deployed runtime source `cc27b609eea2917b97da96003a08e5c84a7edbb1`, `linux/arm64`, immutable LOCAL_LAN endpoints and local auth. Accepted artifact `9558305055` has GitHub/local SHA256 `25057b85a6b096275d3cadef8f03f3dfa8e608e1a3160932aec73d3a368fe74f`; the inner recovery bundle SHA256 is `587b88b53634084be89f1d1fd96c96eb77549655a6f8dbe76d57c16e8c818517`. Manifest/provenance record ARM64, runtime source `cc27b609...`, tooling `431f2a28...`, local auth, no packaged secrets, no mandatory runtime network/paid service and no volume deletion. Hosted update/rollback preserved named-volume identities and local-auth refresh/session/RBAC continuity, including rollback logout revocation.

## Issue #832 — current-source ARM64/local-auth recovery refresh

Issue #832 is repository-complete for the hosted/isolated software-package boundary. Offline Bundle run `33653777090` used tooling `151fbc17f6ba41efb0e5deda474bbadd32fb435e` and exact deployed runtime source `893573a86faf6629e5e3cefc0e41c47230a7f8c7` on `linux/arm64` with local auth. The disconnected stack started with container egress blocked and pull disabled; local-auth continuity passed; update and rollback each preserved all required named volumes, markers and authentication continuity. Accepted artifact `9856886787` is 566,760,176 bytes with artifact SHA-256 `4646304898dba1809bb649fc6c3950c918b6ce291240e525507e7a600105e11b`; recovery bundle SHA-256 is `234b93648d25245730c0c845c6a3cd267625d07c1f4dfbaf263fb552b1257ea1`; captured manifest SHA-256 is `1589c151715a4309a8ad75887251d14b025e6e860531f13b3fe477ace92e589d`. No Raspberry Pi restart, production restore/cutover, Modbus/hardware write, persistent-data deletion or named-volume deletion occurred.

## Issue #189 recovery boundary

The stale hosted recovery-package lineage is cleared by Issue #832: the currently authoritative deployed source `893573a86faf6629e5e3cefc0e41c47230a7f8c7` now has GREEN hosted ARM64/local-auth disconnected-runtime and update/rollback preservation evidence. Issue #189 remains blocked on the **actual-host** acceptance boundary: current-source central/edge reboot and recovery evidence plus controlled power-loss behavior where explicitly approved and safe. Hosted/QEMU evidence must not be represented as actual Raspberry Pi power-loss acceptance.

## Current soft blockers and operational observations

- #723 CI routing maintenance and #719 security reconciliation are completed and merged; #722 triage is closed. Their exact-head verification is durable evidence for the current presentation baseline.
- #696 replaced the refrigeration structural-snapshot history scan with the bounded `telemetry_latest` projection. Real Raspberry Pi SQL evidence improved the affected 84-channel KK2 lookup from `85874.034 ms` to `1.853 ms`; the previous #698 hosted-runner allocation blocker is cleared.
- Authorized telemetry-retention maintenance on 2026-08-26 created and checksum-verified a PostgreSQL backup before deletion. Confirmed completed cleanup is 3,784,832 old `telemetry_session_contexts` rows plus 250,000 old `telemetry_samples`, followed by `VACUUM FULL ANALYZE telemetry_session_contexts`. Full deletion of all sensor samples before 2026-08-20 was not completed and must not be reported as completed retention.

## Hardware validation backlog

- #200 — the second production adapter `0133F246` and Embraco Unit 2 at 9600 8N2 are now verified through #760; Unit 115 reality, full physical topology inspection, termination/bias/shielding/grounding and remaining duplicate-ID isolation evidence are still hardware-unverified.
- #201 — normal-operation LE-01MP semantics are accepted; controlled restart/power-cycle discontinuity evidence remains pending.
- #202 — representative XJP60D firmware/semantic portability still needs real hardware evidence.
- #585 — W2 / Unit 201 handback remains blocked until external RS-485 ownership is released and physical handback is approved.

## Security maintenance

Issue #829 is completed through merged PR #831 at exact verified head `7435610de95858ed1a454b396752177293dca9c1`; its seven pre-existing HIGH exceptions remain bounded to the consolidated **2026-09-05** review. Issue #834 is also completed through merged PR #836 at exact verified head `7d4b0c8b55da862b57c16358126cfd7c856445da`; Container Supply Chain run `33689268672`, Telemetry Service run `33689268665`, Core Quality/build and NEXOLAB Merge Gate run `33689268663` are GREEN with zero unresolved review threads. #834 adds only two exact HIGH-only Telemetry exceptions for `libsystemd0/libudev1 + CVE-2026-16742` through 2026-09-05 after proving the `systemd-homed` path absent and unsafe package purge rejected. TG-02 retains 17 exact Telegram Gateway HIGH-only exceptions under the same consolidated review boundary. None of these repository decisions authorizes production deployment, Modbus write or hardware write.

## Issue #707 Saved Dashboard export and chart workspace

Repository-side software verification is complete for exact product/test head `1ca06521628e5936c9c00fb94ef895d6abfa1c21`. The persisted CSV endpoint accepts the legacy browser identifier `Europe/Kiev` through explicit canonicalization to `Europe/Kyiv` while invalid identifiers remain fail-closed. Saved Dashboard line/area series now consolidate across equipment and split only when the canonical five-axis readability budget is exceeded, preserving persisted series order; value/gauge-only dashboards do not create empty chart panels. Core CI, Telemetry service, Authenticated Dashboard Acceptance, Offline Bundle, Container Supply Chain and NEXOLAB Merge Gate are GREEN, and the fresh Codex review is clean. Generic Live Data equipment-centric grouping remains unchanged.

#707 was subsequently deployed through the controlled LAN path at `/home/nexolab/nexolab-platform/runtime/deployments/20260827T101743Z` and passed deployment readiness/smoke gates. Operator acceptance confirmed the consolidated graphs and CSV download action. CSV content validation exposed #709: temperature-controller telemetry rows were omitted because Live Dashboard resolved catalog identities such as `DIXELL-108` while persisted telemetry uses `K108`. No Modbus or hardware write occurred.

## Issue #709 Saved Dashboard sensor export identity regression

Repository implementation remains verified/merged through PR #710. Production runtime acceptance is now complete on deployed source `ff86b10b...` using the pre-existing active Saved Dashboard `Issue 369 Pi Acceptance`, not seeded/mock telemetry. The deployed exporter returned 359 real rows over 2h, including 120 `108-01 / temperature.probe` rows keyed as `K108`, all numeric and valid with range 24.2–24.6 °C. The Live Dashboard inventory/latest query also resolved `108-01` as `K108` with latest 24.4 °C valid. The former header-only temperature-controller CSV defect is therefore closed on the Raspberry Pi. GitHub Issue #709 was closed completed on 2026-08-29.

## Issue #711 Energy history continuity regression

Repository implementation remains verified on product head `da3569969ad39be4e409fe91bc0821e2587368a0` and is included in deployed historical-main target `ff86b10b...`. Real post-deployment verification used 8,573 raw 24h active-power records from the three live meters and acquisition-registry revision 18. Exact continuity/downsampling logic produced 717 render records with 7 durable breaks and 0 inferred breaks, compared with the pre-fix 448 false breaks observed for the same 717-record shape. Remaining breaks correspond to genuine long silent/runtime interruption boundaries, while normal ~30 s cadence jitter is no longer segmented. No interpolation, acquisition-cadence mutation, Modbus write, hardware write or persistence mutation was introduced.

## Runtime and safety boundary

Core NEXOLAB remains `LOCAL_LAN` / offline-first with no mandatory public internet, paid runtime service, CDN, remote font or external runtime API.

#679 hosted-QEMU acceptance is completed; its GREEN evidence does not authorize Raspberry Pi runtime mutation. Package authority/staging/activation, `establish-package-authority`, source→packaged transition and actual-host update/rollback remain production cutover boundaries requiring separate approval. No destructive restore, persistent-data deletion, named-volume deletion, Modbus/controller write or hardware write is authorized.
