[pytest]
markers =
    integration: requires a real PostgreSQL database
    e2e: requires PostgreSQL and an external MQTT broker
    database_outage: stops and restarts the PostgreSQL service container
